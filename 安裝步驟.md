# 一坨咖啡網站｜第一次安裝步驟

照順序做一次就好，之後每次打開 cmux 就能直接改。

## 1. 接受 GitHub 邀請
到信箱或 https://github.com/liuyuyun8610/jibucoffee/invitations 按 **Accept invitation**。

## 2. 安裝工具（打開「終端機」，一行一行貼上）
```bash
xcode-select --install          # 會安裝 git，跳出視窗就按「安裝」
brew install gh                 # 沒有 brew 的話，先到 https://brew.sh 貼上首頁那行指令
gh auth login                   # 選 GitHub.com → HTTPS → Login with a web browser，用 voyagechochi 登入
gh auth setup-git
```

## 3. 下載網站到自己的電腦
```bash
mkdir -p ~/Projects && cd ~/Projects
git clone https://github.com/liuyuyun8610/jibucoffee.git
```

## 4. 在 cmux 開這個資料夾
在 cmux 開 `~/Projects/jibucoffee`，啟動 Claude。它會自動讀 `CLAUDE.md`，知道要怎麼改、哪些東西不能碰。

## 之後每次要改網站
直接跟 Claude 說要改什麼，例如「菜單新增一杯燕麥拿鐵 150 元」。Claude 會：
1. 先拿最新版本 → 2. 開新分支修改 → 3. 讓你在本機預覽 → 4. 送出 PR。

PR 合併後，正式站 jibucoffee.vercel.app 大約一分鐘會更新。

## 注意
- 動到資料庫（新增欄位、資料表）的部分要交給 liuyuyun8610 執行，Claude 會在 PR 裡寫清楚。
- 不要把任何密碼或金鑰貼進檔案裡。
